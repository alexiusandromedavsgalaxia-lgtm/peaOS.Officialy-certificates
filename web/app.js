const KEY='peacloud-state-v2';
const PEAOS_MANIFEST='https://raw.githubusercontent.com/alexiusandromedavsgalaxia-lgtm/peaOS/main/peacloud.json';
const CERT_REGISTRY='https://raw.githubusercontent.com/alexiusandromedavsgalaxia-lgtm/peaOS.Officialy-certificates/main/registry.json';
const SYNC_INTERVAL=2000,MIN_PASSWORD_LENGTH=64,PBKDF2_ITERATIONS=600000;
const app=document.querySelector('#app'),authDialog=document.querySelector('#authDialog'),authContent=document.querySelector('#authContent'),accountButton=document.querySelector('#accountButton'),certDialog=document.querySelector('#certDialog'),certForm=document.querySelector('#certForm'),certFormError=document.querySelector('#certFormError');
const emptyState={user:null,certificates:[],wdp:null};let state=load(),integration={status:'connecting',os:null,registry:null,error:null,lastSync:null},syncTimer=null;
function load(){try{return {...emptyState,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...emptyState}}}function save(){localStorage.setItem(KEY,JSON.stringify(state))}function esc(s){return String(s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}function id(prefix){return prefix+'-'+crypto.randomUUID().slice(0,8).toUpperCase()}function initials(name){return name.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()}function b64(bytes){let s='';for(const x of bytes)s+=String.fromCharCode(x);return btoa(s)}function bytes(s){return new TextEncoder().encode(s)}
async function passwordHash(password,salt){const key=await crypto.subtle.importKey('raw',bytes(password),'PBKDF2',false,['deriveBits']);const bits=await crypto.subtle.deriveBits({name:'PBKDF2',salt,iterations:PBKDF2_ITERATIONS,hash:'SHA-512'},key,512);return b64(new Uint8Array(bits))}function randomSalt(){const salt=new Uint8Array(32);crypto.getRandomValues(salt);return salt}async function fetchFresh(url){const separator=url.includes('?')?'&':'?';return fetch(`${url}${separator}_=${Date.now()}`,{cache:'no-store',headers:{Accept:'application/json'}})}
function validateRemote(os,registry){if(!os||os.product!=='peaOS')throw new Error('El manifiesto remoto no pertenece a peaOS.');if(os.certificate_type!=='SigningCertificate'||os.certificate_protocol!=='R35-SHA512')throw new Error('El protocolo remoto no es R35-SHA512.');if(!registry||registry.issuer!=='peaOS.Officialy-certificates'||registry.peaOS_certificate_version!==3)throw new Error('El registro remoto no pertenece al protocolo v3.');if(registry.certificate_type!=='SigningCertificate'||registry.authentication!=='R35-SHA512')throw new Error('El registro no usa certificados R35.');if(os.certificate_version!==registry.peaOS_certificate_version)throw new Error('Las versiones de certificado no coinciden.');if(!Array.isArray(registry.certificates))throw new Error('El registro remoto tiene un formato inválido.');for(const c of registry.certificates)if(!c||typeof c!=='object'||!/^[a-f0-9]{32}$/.test(c.id)||c.type!=='SigningCertificate'||!c.status||!/^[a-f0-9]{128}$/.test(c.sha512||''))throw new Error('El registro contiene una entrada inválida.')}
async function syncPeaOS({silent=false}={}){if(!silent){integration={...integration,status:'connecting',error:null};render()}try{const [or,rr]=await Promise.all([fetchFresh(PEAOS_MANIFEST),fetchFresh(CERT_REGISTRY)]);if(!or.ok||!rr.ok)throw new Error('No se pudo leer el manifiesto o registro remoto.');const [os,registry]=await Promise.all([or.json(),rr.json()]);validateRemote(os,registry);integration={status:'connected',os,registry,error:null,lastSync:new Date()}}catch(error){integration={...integration,status:'offline',error:error.message,lastSync:integration.lastSync}}render()}
function startLiveSync(){if(syncTimer)clearInterval(syncTimer);syncTimer=setInterval(()=>syncPeaOS({silent:true}),SYNC_INTERVAL)}
function render(){const count=state.certificates.length,active=state.certificates.filter(c=>c.status==='Active').length,official=integration.registry?.certificates?.length??0,osVersion=integration.os?.version||'sin conexión',syncText=integration.lastSync?integration.lastSync.toLocaleTimeString('es-ES'):'nunca';const statusText=integration.status==='connected'?'conectado':integration.status==='connecting'?'conectando':'sin conexión';const statusClass=integration.status==='connected'?'online':integration.status==='connecting'?'pending':'offline';document.querySelector('#connectionPill')?.replaceChildren(Object.assign(document.createElement('i'),{className:statusClass}),document.createTextNode(` ${statusText}`));app.innerHTML=`<section id="home" class="hero"><div><div class="eyebrow">PEACLOUD / PEAOS</div><h1>la identidad de firma, sin ruido.</h1><p>Un panel oscuro para consultar el registro oficial, comprobar su estado y gestionar tus borradores locales.</p><div class="actions"><button class="primary" id="heroAccount">${state.user?'Abrir peaCloud':'Crear cuenta peaCloud'} <span>→</span></button><a class="secondary" href="#certificates">Explorar certificados</a></div></div><div class="hero-card"><div class="card-label">ESTADO DEL SISTEMA</div><div class="status-line"><span><i class="status-dot ${statusClass}"></i> peaOS</span><strong>${esc(statusText)}</strong></div><div class="row"><span>Versión</span><strong>${esc(osVersion)}</strong></div><div class="row"><span>Firmas oficiales</span><strong>${official}</strong></div><div class="row"><span>Última sync</span><strong>${esc(syncText)}</strong></div></div></section><section class="section"><div class="section-head"><div><div class="eyebrow">PROTOCOLO</div><h2>R35-SHA512</h2><div class="muted">registro verificable y separado de WDP.</div></div></div><div class="stats"><div class="card stat"><strong>${esc(osVersion)}</strong><span>peaOS</span></div><div class="card stat"><strong>${integration.os?.certificate_version??'—'}</strong><span>cert version</span></div><div class="card stat"><strong>Ed25519</strong><span>algoritmo</span></div><div class="card stat"><strong>${integration.status==='connected'?'OK':'—'}</strong><span>registro remoto</span></div></div><div class="card wdp"><div class="feature-grid"><div><div class="feature-icon">✓</div><h3>fuente oficial</h3><p class="muted">Los datos se leen directamente desde GitHub y se vuelven a consultar automáticamente cada 2 segundos.</p></div><div><div class="feature-icon">⌁</div><h3>integridad</h3><p class="muted">Cada entrada publicada lleva su SHA-512. La clave privada nunca forma parte del registro público.</p></div><div><div class="feature-icon">◌</div><h3>separación</h3><p class="muted">WDP sigue siendo independiente de la identidad criptográfica de firma.</p></div></div><div class="actions"><button class="secondary" id="syncPeaOS">Sincronizar ahora</button><a class="secondary" href="${esc(CERT_REGISTRY)}" target="_blank" rel="noreferrer">Abrir registry.json ↗</a></div>${integration.error?`<div class="notice">${esc(integration.error)}</div>`:''}</div></section><section id="certificates" class="section"><div class="section-head"><div><div class="eyebrow">REGISTRY</div><h2>certificados de firma</h2><div class="muted">oficiales remotos + tus borradores locales.</div></div><button class="primary" id="newCert">+ Nuevo certificado</button></div><div class="stats compact"><div class="card stat"><strong>${official}</strong><span>oficiales</span></div><div class="card stat"><strong>${count}</strong><span>borradores</span></div><div class="card stat"><strong>${active}</strong><span>activos locales</span></div></div><div class="cert-grid">${integration.registry?.certificates?.map(officialCertCard).join('')||''}${count?state.certificates.map(certCard).join(''):''}${!official&&!count?`<div class="card empty">No hay certificados publicados todavía.<br><button class="secondary" id="emptyCreate">Crear un borrador local</button></div>`:''}</div></section><section id="wdp" class="section"><div class="card wdp"><div class="eyebrow">WEB DISTRIBUTION PROGRAM</div><h2>WDP va por separado.</h2><p class="muted">Las solicitudes de distribución web no se mezclan con el certificado criptográfico. Aquí solo se guardan borradores locales.</p><button class="primary" id="wdpButton">${state.wdp?'Ver borrador WDP':'Crear borrador WDP'} <span>→</span></button>${state.wdp?`<div class="notice">${esc(state.wdp.origin)} · ${esc(state.wdp.id)}</div>`:''}</div></section><footer class="footer"><b>peaCloud</b> · R35-SHA512 · GitHub registry · sincronización automática · ${new Date().getFullYear()}</footer>`;document.querySelector('#heroAccount').onclick=()=>state.user?showAccount():openAuth('signup');document.querySelector('#newCert').onclick=()=>state.user?certDialog.showModal():openAuth('signup');document.querySelector('#emptyCreate')?.addEventListener('click',()=>state.user?certDialog.showModal():openAuth('signup'));document.querySelector('#wdpButton').onclick=()=>state.user?showWdp():openAuth('signup');document.querySelector('#syncPeaOS').onclick=()=>syncPeaOS();updateAccountButton()}
function officialCertCard(c){return `<article class="card cert"><div class="cert-top"><span class="badge">${esc(c.status||'Unknown')}</span><span class="cert-type">SigningCertificate</span></div><h3>${esc(c.id)}</h3><div class="cert-meta"><div><span>issuer key</span><b>${esc(c.issuer_key_id||'—')}</b></div><div><span>serial</span><b>${esc(c.serial||'—')}</b></div><div><span>SHA-512</span><code>${esc(c.sha512||'—')}</code></div><div><span>vigencia</span><b>${esc(c.issued_at||'—')} → ${esc(c.expires_at||'—')}</b></div></div><div class="source">● GitHub R35 · oficial</div></article>}
function certCard(c){return `<article class="card cert local"><div class="cert-top"><span class="badge local-badge">Borrador local</span></div><h3>${esc(c.name)}</h3><div class="cert-meta"><div><span>ID</span><b>${esc(c.id)}</b></div><div><span>protocolo</span><b>R35-SHA512 · Ed25519</b></div><div><span>creado</span><b>${new Date(c.createdAt).toLocaleString('es-ES')}</b></div></div><button class="secondary" data-revoke="${esc(c.id)}">Eliminar borrador</button></article>`}
function updateAccountButton(){accountButton.innerHTML=state.user?`<span class="signed"><span class="avatar">${initials(state.user.name)}</span>${esc(state.user.name)}</span>`:'Iniciar sesión';accountButton.onclick=()=>state.user?showAccount():openAuth('login')}
function openAuth(mode){renderAuth(mode);authDialog.showModal()}function renderAuth(mode){authContent.innerHTML=mode==='login'?`<div class="dialog-icon">↗</div><h2>Iniciar sesión</h2><p class="muted">Tu cuenta actual es local a este navegador.</p><label>Correo<input id="authEmail" type="email" required autocomplete="email"></label><label>Contraseña<input id="authPassword" type="password" required minlength="${MIN_PASSWORD_LENGTH}" autocomplete="current-password"></label><button class="primary" id="authSubmit">Entrar <span>→</span></button><p id="authError" class="error"></p><p class="switch">¿No tienes cuenta? <button id="switchAuth">Crear cuenta</button></p>`:`<div class="dialog-icon">✦</div><h2>Crear cuenta peaCloud</h2><p class="muted">Cuenta local. Contraseña larga y única de al menos ${MIN_PASSWORD_LENGTH} caracteres.</p><label>Nombre<input id="authName" required maxlength="60" autocomplete="name"></label><label>Correo<input id="authEmail" type="email" required autocomplete="email"></label><label>Contraseña<input id="authPassword" type="password" required minlength="${MIN_PASSWORD_LENGTH}" autocomplete="new-password"></label><button class="primary" id="authSubmit">Crear cuenta <span>→</span></button><p id="authError" class="error"></p><p class="switch">¿Ya tienes cuenta? <button id="switchAuth">Iniciar sesión</button></p>`;document.querySelector('#authSubmit').onclick=()=>submitAuth(mode);document.querySelector('#switchAuth').onclick=()=>renderAuth(mode==='login'?'signup':'login')}
async function submitAuth(mode){const email=document.querySelector('#authEmail').value.trim().toLowerCase(),password=document.querySelector('#authPassword').value,error=document.querySelector('#authError');if(!email||password.length<MIN_PASSWORD_LENGTH){error.textContent=`Usa un correo y una contraseña de al menos ${MIN_PASSWORD_LENGTH} caracteres.`;return}if(mode==='signup'){const name=document.querySelector('#authName').value.trim();if(!name){error.textContent='Escribe tu nombre.';return}const salt=randomSalt(),hash=await passwordHash(password,salt);state.user={name,email,password:{algorithm:'PBKDF2-HMAC-SHA-512',iterations:PBKDF2_ITERATIONS,salt:b64(salt),hash}};save();authDialog.close();render();return}if(!state.user||state.user.email!==email||!state.user.password){error.textContent='No existe una cuenta local válida con ese correo.';return}const salt=Uint8Array.from(atob(state.user.password.salt),c=>c.charCodeAt(0)),hash=await passwordHash(password,salt);if(hash!==state.user.password.hash){error.textContent='Correo o contraseña incorrectos.';return}authDialog.close();render()}
function showAccount(){authContent.innerHTML=`<div class="dialog-icon">●</div><h2>Tu peaCloud local</h2><p class="muted">${esc(state.user.email)}</p><div class="hero-card"><div class="row"><span>Cuenta</span><strong>${esc(state.user.name)}</strong></div><div class="row"><span>Borradores</span><strong>${state.certificates.length}</strong></div><div class="row"><span>Protección</span><strong>PBKDF2-HMAC-SHA-512</strong></div></div><button class="secondary" id="logout">Cerrar sesión</button>`;authDialog.showModal();document.querySelector('#logout').onclick=()=>{state.user=null;save();authDialog.close();render()}}
function revoke(certId){const c=state.certificates.find(x=>x.id===certId);if(c){c.status='Revoked';c.revokedAt=new Date().toISOString();save();render()}}function showWdp(){authContent.innerHTML=state.wdp?`<div class="dialog-icon">⌁</div><h2>Borrador WDP</h2><p class="muted">Registro local independiente del certificado de firma.</p><div class="hero-card"><div class="row"><span>ID</span><strong>${esc(state.wdp.id)}</strong></div><div class="row"><span>Origen</span><strong>${esc(state.wdp.origin)}</strong></div></div>`:`<div class="dialog-icon">⌁</div><h2>Crear borrador WDP</h2><p class="muted">Esto no crea un certificado.</p><label>Origen web<input id="wdpOrigin" placeholder="https://example.com" required></label><button class="primary" id="wdpSubmit">Guardar borrador <span>→</span></button><p id="wdpError" class="error"></p>`;authDialog.showModal();document.querySelector('#wdpSubmit')?.addEventListener('click',()=>{const origin=document.querySelector('#wdpOrigin').value.trim(),error=document.querySelector('#wdpError');if(!/^https:\/\//i.test(origin)){error.textContent='Usa un origen https:// válido.';return}state.wdp={id:id('DRAFT-WDP'),origin,createdAt:new Date().toISOString(),status:'Draft'};save();authDialog.close();render()})}
certForm.addEventListener('submit',e=>{e.preventDefault();if(!state.user){certFormError.textContent='Inicia sesión primero.';return}const name=new FormData(certForm).get('name')?.toString().trim();if(!name){certFormError.textContent='Escribe un nombre.';return}state.certificates.unshift({id:id('SIGN-DRAFT'),name,status:'Active',type:'SigningCertificate',protocol:'R35-SHA512',algorithm:'Ed25519',createdAt:new Date().toISOString(),owner:state.user.email});save();certDialog.close();certForm.reset();render()});document.addEventListener('click',e=>{const b=e.target.closest('[data-revoke]');if(b)revoke(b.dataset.revoke)});render();syncPeaOS();startLiveSync();
